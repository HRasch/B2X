# Production Environment Variables
# Infrastructure ports are NOT exposed in production for security
# Only application ports (443) are exposed via reverse proxy/load balancer

# Database Configuration (internal networking only)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=${POSTGRES_PASSWORD}

# Redis Configuration (internal networking only)
REDIS_PASSWORD=${REDIS_PASSWORD}

# RabbitMQ Configuration (internal networking only)
RABBITMQ_USER=${RABBITMQ_USER}
RABBITMQ_PASSWORD=${RABBITMQ_PASSWORD}

# Elasticsearch Configuration (internal networking only)
ELASTICSEARCH_SECURITY_ENABLED=true
ELASTIC_PASSWORD=${ELASTIC_PASSWORD}

# SSL Certificate (required for production)
SSL_CERT_PASSWORD=${SSL_CERT_PASSWORD}