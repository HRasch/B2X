{
  "name": "b2connect-workspace",
  "version": "1.0.0",
  "description": "B2Connect - Full Stack Application with .NET Aspire Backend and Vue Frontend",
  "private": true,
  "workspaces": [
    "frontend-store",
    "frontend-admin"
  ],
  "scripts": {
    "install:all": "npm install && npm install --prefix frontend-store && npm install --prefix frontend-admin",
    "dev": "concurrently \"npm --prefix frontend-store run dev\" \"npm --prefix frontend-admin run dev\" --names \"Store,Admin\" --prefix name --kill-others-on-exit",
    "dev:frontend": "npm --prefix frontend-store run dev",
    "dev:admin": "npm --prefix frontend-admin run dev",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces",
    "type-check": "npm run type-check --workspaces"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "keywords": [
    "b2connect",
    "vue",
    "aspire",
    "microservices"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@rollup/rollup-darwin-arm64": "^4.54.0",
    "concurrently": "^9.2.1"
  }
}
