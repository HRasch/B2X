{
  "name": "b2connect-workspace",
  "version": "1.0.0",
  "description": "B2Connect - Full Stack Application with .NET Aspire Backend and Vue Frontend",
  "private": true,
  "workspaces": [
    "frontend/Store",
    "frontend/Admin",
    "frontend/Management"
  ],
  "scripts": {
    "install:all": "npm install && npm install --prefix frontend/Store && npm install --prefix frontend/Admin && npm install --prefix frontend/Management",
    "dev": "concurrently \"npm --prefix frontend/Store run dev\" \"npm --prefix frontend/Admin run dev\" \"npm --prefix frontend/Management run dev\" --names \"Store,Admin,Tenant\" --prefix name --kill-others-on-exit",
    "dev:frontend": "npm --prefix frontend/Store run dev",
    "dev:admin": "npm --prefix frontend/Admin run dev",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces",
    "type-check": "npm run type-check --workspaces",
    "format": "prettier --write \"frontend/**/*.{js,ts,vue,json,css,scss,md}\"",
    "format:check": "prettier --check \"frontend/**/*.{js,ts,vue,json,css,scss,md}\"",
    "format:backend": "dotnet format B2Connect.slnx",
    "format:all": "npm run format && npm run format:backend && npm run format:tools",
    "format:tools": "for dir in tools/TypeScriptMCP tools/VueMCP tools/HtmlCssMCP tools/SecurityMCP tools/WolverineMCP tools/B2ConnectMCP tools/PlaywrightMCP; do [ -d \"$dir\" ] && (cd \"$dir\" && npm run format 2>/dev/null) || true; done",
    "check:backend": "dotnet format B2Connect.slnx --verify-no-changes",
    "check:tools": "for dir in tools/TypeScriptMCP tools/VueMCP tools/HtmlCssMCP tools/SecurityMCP tools/WolverineMCP tools/B2ConnectMCP tools/PlaywrightMCP; do [ -d \"$dir\" ] && (cd \"$dir\" && npm run format:check 2>/dev/null) || true; done",
    "check:all": "npm run format:check && npm run check:backend && npm run check:tools",
    "legacy-cleanup": "node scripts/legacy-code-cleanup.js",
    "prepare": "husky"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "keywords": [
    "b2connect",
    "vue",
    "aspire",
    "microservices"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.2.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.3.3"
  },
  "lint-staged": {
  "lint-staged": {
    "*.cs": [
      "dotnet format --include"
    ],
    "tools/**/*.{ts,js,json}": [
      "prettier --write --ignore-unknown",
      "eslint --fix"
    ],
    "frontend/**/*.{js,ts,vue}": [
      "prettier --write",
      "eslint --fix"
    ],
    "frontend/**/*.{json,css,scss,md}": [
      "prettier --write"
    ]
  },
    "frontend/**/*.{json,css,scss,md}": [
      "prettier --write"
    ],
    "frontend/**/*.{ts,vue}": [
      "vue-tsc --noEmit"
    ]
  },
  "dependencies": {
    "js-yaml": "^4.1.1"
  }
}
