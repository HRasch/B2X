<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>B2X.ERP.Abstractions</RootNamespace>
    
    <Description>
      ERP abstraction contracts for HTTP communication between B2X (.NET 10) 
      and ERP Connector (.NET Framework 4.8). Supports massive data loads via 
      cursor-based pagination, delta sync, chunked streaming, and batch operations.
    </Description>
  </PropertyGroup>

  <ItemGroup>
    <!-- Core serialization - compatible with .NET 4.8 via NuGet -->
    <PackageReference Include="System.ComponentModel.Annotations" />
  </ItemGroup>

  <ItemGroup>
    <!-- Shared ERP Core for common interfaces and models (ADR-036) -->
    <ProjectReference Include="..\..\..\shared\B2X.Shared.Erp.Core\B2X.Shared.Erp.Core.csproj" />

  </ItemGroup>

</Project>


