<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFrameworks>netstandard2.1;net48;net8.0;net10.0</TargetFrameworks>
    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>B2Connect.Shared.Erp.Validation</RootNamespace>
    <AssemblyName>B2Connect.Shared.Erp.Validation</AssemblyName>
    <Description>Shared ERP validation logic for B2Connect multi-ERP architecture</Description>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <!-- Disable CPM for multi-targeting compatibility packages -->
    <ManagePackageVersionsCentrally>false</ManagePackageVersionsCentrally>
  </PropertyGroup>

  <!-- Conditional ImplicitUsings for .NET Standard -->
  <PropertyGroup Condition="'$(TargetFramework)' == 'netstandard2.1' OR '$(TargetFramework)' == 'net48'">
    <ImplicitUsings>disable</ImplicitUsings>
  </PropertyGroup>

  <ItemGroup Condition="'$(TargetFramework)' == 'netstandard2.1' OR '$(TargetFramework)' == 'net48'">
    <PackageReference Include="System.Threading.Tasks.Extensions" Version="4.5.4" />
    <PackageReference Include="Microsoft.Bcl.AsyncInterfaces" Version="8.0.0" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\B2Connect.Shared.Erp.Core\B2Connect.Shared.Erp.Core.csproj" />
  </ItemGroup>

</Project>
