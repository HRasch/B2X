# ===== SECURITY: SECRETS =====
# ⚠️ CRITICAL: In Production NIEMALS in .env speichern!
# Verwenden Sie stattdessen: Azure Key Vault, AWS Secrets Manager, Kubernetes Secrets, oder Docker Secrets
# Reference: https://docs.microsoft.com/en-us/azure/key-vault/

# JWT Configuration (mindestens 32 Zeichen, mit Sonderzeichen)
# Generate secure value: openssl rand -base64 32
# Example: MySecure$ecret123!@#$%^&*KeyForProduction2024
JWT_SECRET=<generate-secure-random-string-at-least-32-chars>
JWT_ISSUER=B2Connect
JWT_AUDIENCE=B2Connect
JWT_TOKEN_EXPIRATION_MINUTES=60
JWT_REFRESH_TOKEN_EXPIRATION_DAYS=7

# ===== ASPNETCORE =====
ASPNETCORE_ENVIRONMENT=Development
ASPNETCORE_URLS=http://+:8080;https://+:8090

# ===== DATABASE =====
Database__Provider=inmemory
# For PostgreSQL (MUST change password in production!):
# Generate secure password: openssl rand -base64 32
POSTGRES_USER=postgres
POSTGRES_PASSWORD=<use-secure-random-password>
POSTGRES_DB=b2connect

# ===== CORS =====
CORS__ALLOWEDORIGINS__0=http://localhost:5174
CORS__ALLOWEDORIGINS__1=http://127.0.0.1:5174
CORS__ALLOWEDORIGINS__2=http://localhost:5173
CORS__ALLOWEDORIGINS__3=http://127.0.0.1:5173

# ===== LOGGING =====
Logging__LogLevel__Default=Information
Logging__LogLevel__Microsoft.AspNetCore=Warning

# ===== API GATEWAY =====
API_GATEWAY_PORT=5000
AUTH_SERVICE_URL=http://auth-service:5001
TENANT_SERVICE_URL=http://tenant-service:5002

# ===== FRONTEND =====
VITE_API_URL=http://localhost:5000/api
VITE_ENV=development
NODE_ENV=development

# ===== PRODUCTION CONFIGURATION EXAMPLE =====
# ⚠️ Never commit actual secrets!
# Set these via cloud secret managers in production:
# JWT_SECRET=<from-azure-keyvault-or-aws-secrets>
# DATABASE_CONNECTION_STRING=<from-keyvault>
# CORS__ALLOWEDORIGINS__0=https://admin.b2connect.com
# CORS__ALLOWEDORIGINS__1=https://store.b2connect.com
