# B2X Development Environment Configuration
# ==========================================
# This file configures development settings per machine.
# Copy to .vscode/b2x-dev.local.env for machine-specific overrides (gitignored).

# CPU Parallelization (0 = auto-detect all cores)
# Override for slower machines or when running other intensive tasks
B2X_MAX_CPU_COUNT=0

# .NET Build Settings
DOTNET_NUGET_SIGNATURE_VERIFICATION=false
DOTNET_SKIP_FIRST_TIME_EXPERIENCE=true
DOTNET_CLI_TELEMETRY_OPTOUT=true

# MSBuild Performance
MSBUILDTERMINALLOGGER=auto
MSBUILDNODECOUNT=0

# Aspire Settings
ASPNETCORE_ENVIRONMENT=Development
DOTNET_DASHBOARD_UNSECURED_ALLOW_ANONYMOUS=true
ASPIRE_ALLOW_UNSECURED_TRANSPORT=true
